import React from 'react';
import Loading from '../components/Loading';
import { useParams, Link } from 'react-router-dom';
const characterUrl = '';

const SingleCharacter = () => {
    return (
        <div>
            <h2>single character page</h2>
        </div>
    );
}

export default SingleCharacter;